<h1> <%= @airline.name %>'s Show Page </h1>

<h2>Adult Passengers</h2>
<div id = 'unique-info'>
    <ul>
        <% @adult_passengers.each do |passenger| %>
            <li><%= passenger.name %> Age: <%= passenger.age %></li>
        <% end %>
    </ul>
</div>