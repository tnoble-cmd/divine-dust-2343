<h1> Flight Index Page </h1>
<div id = "flight-info-list">
    <% @flights.each do |flight| %>
        <ul>
            <li>Flight Number: <%= flight.number %></p> - Airline: <%= flight.airline.name %></li>
        </ul>
        <ul>
            <% flight.passengers.each do |passenger| %>
                <li>Passenger: <%= passenger.name %></li> 
                <%= link_to "Remove Passenger", flight_flight_passenger_path(flight, passenger), method: :delete, class: 'remove-passenger' %>
            <% end %>
        </ul>
    <% end %>
</div>